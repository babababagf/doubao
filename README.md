# 星枢豆包获客｜商户网页端 UI

这是从主项目独立出来的前端设计分支，只用于优化商户网页客户端 UI。

## 本地运行

```bash
pnpm install
pnpm dev
```

默认使用 Mock 数据，访问 `http://127.0.0.1:5180/`，本地演示账号为 `admin`，密码为 `demo1234`。

## 验证

```bash
pnpm typecheck
pnpm test
pnpm build
```

开始修改前请完整阅读 `GEMINI_UI_HANDOFF.md`。当前实现与目标参考图位于 `design/reference/`。
